# Frontend Display Fixes

## Issues

1. **Robot not moving**: Keyboard controls publish to `/cmd_vel_raw` but robot doesn't move
2. **Multiple robots shown**: Duplicate robots rendered on top of each other
3. **Commit previous coupling fix**: Robot/lidar coupling changes need commit

## Scope

- Debug why keyboard velocity commands aren't moving robot
- Fix duplicate robot rendering
- Commit all changes
