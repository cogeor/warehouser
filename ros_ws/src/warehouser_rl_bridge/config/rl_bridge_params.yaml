rl_bridge:
  ros__parameters:
    # Episode limits
    max_steps: 500

    # Reward shaping
    progress_weight: 1.0
    collision_penalty: -100.0
    success_bonus: 100.0
    pickup_bonus: 50.0
    time_penalty: -0.1

    # Goal reached threshold (meters)
    goal_threshold: 0.5
